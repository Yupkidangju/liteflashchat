# LiteFlashChat 변경 이력 (CHANGELOG.md)

본 프로젝트는 SemVer(Semantic Versioning) 규격을 엄격히 준수합니다.

---

## [Unreleased] - 2026-05-28

### Planned (계획 확정)
* **Phase 12 구조 정리 로드맵 확정:**
  - `src/App.tsx` 2439줄, `src/index.css` 1076줄, `main.go` 592줄, `proxy.go` 723줄 기준선을 문서화했습니다.
  - 순수 유틸, API 클라이언트, 상태 훅, UI 컴포넌트, 스타일, 백엔드 프록시 파일 분리 순서를 확정했습니다.
  - Phase 12는 기능 추가가 아니라 동작 무변경 리팩터링으로 고정하고, API endpoint, 저장 키, export schemaVersion, provider ID를 유지하는 정책을 명시했습니다.

### Changed (구조 개선)
* **Phase 12 구조 정리 완료:**
  - `src/App.tsx`를 2439줄 기준선에서 862줄로 축소하고, 화면 조립자 역할 중심으로 정리했습니다.
  - `src/index.css`를 1076줄 기준선에서 82줄로 축소하고, 기능별 스타일을 `src/styles/*`로 분리했습니다.
  - `main.go`를 592줄 기준선에서 62줄 서버 부트스트랩으로 축소하고, 기존 `proxy.go`의 대형 프록시 구현을 책임별 Go 파일로 분리했습니다.
  - Sidebar, ChatHeader, MessageList, InputPanel, ParamsPanel, SettingsModal, ModelSearchModal을 `src/components/*`로 분리했습니다.
  - 세션, 압축률, 모델 보정, 프리셋, Inspector 상태 저장 축을 `src/hooks/*`로 분리했습니다.
  - 스트리밍 채팅 실행/컨텍스트 압축/Inspector 기록은 `src/services/chatFlow.ts`, JSON/Markdown 데이터 이식은 `src/services/dataPortability.ts`로 분리했습니다.
  - provider/model/prompt API 클라이언트와 순수 유틸을 `src/api/*`, `src/utils/*`로 분리했습니다.

### Changed (변경된 문서)
* **AI 구현 문서 표준 기준 동기화:**
  - `spec.md`, `implementation_summary.md`, `audit_roadmap.md`, `DESIGN_DECISIONS.md`, `designs.md`에 Phase 12 구현 단위, 검증 기준, ADR, UI 컴포넌트 경계와 완료 상태를 반영했습니다.

---

## [1.4.0] - 2026-05-28

### Added (추가된 기능)
* **API 채팅 플랫폼 확장 구현 완료:**
  - `/api/chat/stream` SSE 프록시를 추가하고 기본 채팅 전송을 스트리밍 응답 중심으로 전환했습니다.
  - assistant 메시지 하나에 delta를 누적 표시하고, `[전송]` 버튼을 스트리밍 중 `[중지]`로 전환하여 요청 취소와 부분 응답 보존을 지원합니다.
  - 대화 JSON/Markdown 내보내기, JSON 가져오기 병합, 좌측 대화 검색, 우측 요청/응답 Inspector, 채팅 프리셋 저장/적용/이름 변경/삭제를 추가했습니다.
  - JSON 가져오기는 기존 데이터를 삭제하지 않는 병합 방식이며, Inspector와 내보내기는 API Key/Authorization/암호문을 제외합니다.

### Changed (변경된 사항)
* **패키지 버전 갱신:**
  - `package.json`과 `package-lock.json` 버전을 `1.4.0`으로 갱신했습니다.

### Changed (변경된 문서)
* **AI 구현 문서 표준 기준 구현 동기화:**
  - `spec.md`, `designs.md`, `implementation_summary.md`, `DESIGN_DECISIONS.md`, `BUILD_GUIDE.md`, `audit_roadmap.md`, `README.md`, `LESSONS_LEARNED.md`에 v1.4.0 구현 완료 기준을 반영했습니다.
  - `audit_roadmap.md`의 Phase 11을 구현 완료 감사 기준으로 갱신했습니다.

## [1.3.1] - 2026-05-28

### Fixed (수정된 버그)
* **모델 컨텍스트 메타데이터 보정:**
  - OpenAI 호환 모델 목록에 명시 컨텍스트가 없을 때 임의 `8192` 값을 넣지 않고 `알 수 없음` 상태로 표시하도록 수정했습니다.
  - LM Studio는 `/api/v0/models`의 `max_context_length`를 추가 조회하여 `/v1/models` 결과와 병합하도록 구현했습니다.
  - 메타데이터 출처(`metadataSource`)와 추정 여부(`isContextEstimated`)를 UI에 표시하고, 불명확한 모델은 설정에서 수동 보정할 수 있게 했습니다.

### Changed (변경된 사항)
* **기본 생성 파라미터 정정:**
  - 기본값을 `temperature=1`, `top_p=0.95`, `top_k=40`, `repetition_penalty=1.1`로 변경했습니다.
* **컨텍스트 압축 기준 정정:**
  - 압축 트리거와 입력창 위 표시 기준을 전체 컨텍스트가 아니라 모델의 입력 한도(`maxInputTokens`)로 맞췄습니다.
  - 입력/출력/전체 컨텍스트 값은 모델 메타데이터 또는 사용자의 수동 보정값이 있을 때만 확정값으로 적용합니다.

## [1.3.0] - 2026-05-27

### Added (추가된 기능)
* **모델 메타데이터 기반 한도 적용 구현 완료:**
  - 모델 선택 시 `contextLength`, `maxInputTokens`, `maxOutputTokens`, `supportedParameters`를 적용하는 백엔드/프론트엔드 계약을 문서화했습니다.
  - OpenRouter는 확정 메타데이터를 사용하고, OpenAI 호환 계열은 응답에 명시된 정보만 활성화하는 정책을 확정했습니다.
* **대화 제목 자동/수동 저장 구현 완료:**
  - 첫 사용자 메시지를 자동 제목으로 저장하고, 사용자가 수동 변경한 제목은 이후 자동 갱신하지 않는 `isTitleAutoGenerated` 정책을 추가했습니다.
* **우측 모델 파라미터 패널 구현 완료:**
  - `temperature`, `top_p`, `top_k`, `repetition_penalty`를 모델 지원 여부에 따라 활성/비활성화하는 UI 계약을 추가했습니다.
* **컨텍스트 표시 및 LLM 요약 압축 구현 완료:**
  - 입력창 위 예상 컨텍스트 표시, 기본 압축 비율 `0.7`, LLM 요약 기반 압축 흐름, 실패 시 전송 중단 정책을 문서화했습니다.

### Changed (변경된 문서)
* **AI 구현 문서 표준 기준 문서 동기화:**
  - `spec.md`, `designs.md`, `implementation_summary.md`, `DESIGN_DECISIONS.md`, `BUILD_GUIDE.md`, `audit_roadmap.md`, `README.md`, `LESSONS_LEARNED.md`에 v1.3.0 구현 기준 로드맵을 반영했습니다.
  - `audit_roadmap.md`의 Phase 9를 실제 구현 순서, 검증 명령, 수동 확인 기준까지 닫힌 구현 감사 프레임으로 확정했습니다.

## [1.2.4] - 2026-05-27

### Fixed (수정된 버그)
* **프로바이더 저장 직후 미반영 문제 해결:**
  - 설정 저장 성공 후 `/api/providers`를 즉시 재조회하고 저장 대상 프로바이더를 활성 상태와 현재 대화방 상태에 반영하도록 수정했습니다.
  - 저장된 프로바이더가 활성화 가능한 상태이면 모델 목록을 즉시 다시 불러와 새로고침 없이 모델 선택으로 이어지게 했습니다.

### Changed (변경된 사항)
* **사이드바 프로바이더 편집 목록 제거:**
  - 좌측 하단의 프로바이더명, Edit 버튼, 활성 LED 목록을 제거하고 프로바이더 상태 확인과 수정 책임을 설정 모달로 단일화했습니다.
* **Super Prompt 설정/장착 분리:**
  - 설정 모달은 Super Prompt 생성, 편집, 삭제만 담당하고 대화방 장착/해제는 좌측 사이드바 선택 컨트롤에서만 수행하도록 변경했습니다.
* **설정 모달 명도 대비 개선:**
  - 설정 탭, 라벨, 입력 필드, 목록 텍스트의 색상 토큰을 조정하여 어두운 배경에서 읽기 쉬운 대비를 확보했습니다.
* **AI 구현 문서 표준 동기화:**
  - `AI_IMPLEMENTATION_DOC_STANDARD.md` 기준에 맞춰 `spec.md`에 상태 플래그, CTA 후속 상태, 런타임/빌드 파이프라인, 구현 순서, 검증 기준, 잔여 리스크를 보강했습니다.
  - `DESIGN_DECISIONS.md`에 v1.2.4 ADR을 추가하고, 사이드바 프로바이더 편집 전제와 오래된 탭 명칭을 현재 구현 기준으로 정리했습니다.

---

## [1.2.3] - 2026-05-27

### Added (추가된 기능)
* **범용 output 빌드 스크립트 추가:**
  - `scripts/build-output.mjs`를 추가하여 Windows, macOS, Linux에서 동일하게 `npm run build:output`으로 패키징할 수 있게 했습니다.
  - 빌드 결과는 `output/` 폴더에 저장되며, 현재 운영체제용 실행 파일과 `dist/` 정적 자산을 함께 배치합니다.

### Changed (변경된 사항)
* **빌드 산출물 관리 정책 정리:**
  - `output/` 폴더를 Git 제외 대상으로 추가했습니다.
  - 로컬 비밀 파일과 런타임 설정 파일은 `output/`에 복사하지 않는 정책을 문서화했습니다.

---

## [1.2.2] - 2026-05-27

### Fixed (수정된 버그)
* **키 암호/복호화 보존성 강화:**
  - `crypto_test.go`가 실제 `.secret.key`를 삭제하던 위험 경로를 제거하고, 테스트 전용 임시 키 파일만 사용하도록 격리했습니다.
  - `__KEEP_EXISTING__` 저장 시 기존 암호문을 먼저 복호화 검증하여 깨진 키를 조용히 유지하지 않도록 차단했습니다.
* **Base URL 중복 엔드포인트 저장 방지:**
  - `/models` 또는 `/chat/completions`가 붙은 URL을 저장 전에 API 루트로 정규화하여 모델 조회와 채팅 프록시가 이중 경로로 깨지지 않게 했습니다.
* **프론트엔드 품질 게이트 복구:**
  - React Hooks lint 오류와 명시적 `any` 타입을 제거하여 `npm run lint`를 통과하도록 정리했습니다.

### Changed (변경된 사항)
* **프로바이더 상태 응답 확장:**
  - `/api/providers` 응답에 `configStatus`, `statusMessage`를 추가하여 `missing`, `ready`, `invalid_key` 상태를 UI가 구분할 수 있게 했습니다.
* **로컬 프로바이더 설정 정책 개선:**
  - `lm_studio`, `local_llm`은 API Key 입력 없이 Base URL만 저장해도 내부 더미 토큰으로 모델 조회 흐름을 유지합니다.
* **빌드 명령 표준화:**
  - 새 백엔드 파일 추가와 현재 작업공간의 VCS 메타데이터 불완전 상태에 안전하도록 Go 실행/빌드 명령을 `go run -buildvcs=false .`, `go build -buildvcs=false -o liteflashchat .` 기준으로 변경했습니다.

### Roadmap (확정 구현 단위)
* **완료:** 키 파일 테스트 격리, Base URL 정규화, 기존 키 복호화 검증, 로컬 프로바이더 무키 저장, 모델 로드 오류 UI 표시, lint 복구.
* **다음 점검 단위:** 실제 LM Studio 또는 OpenAI 호환 로컬 서버를 켠 상태에서 `저장 -> 모델 목록 -> 모델 선택 -> 채팅` 수동 E2E를 수행합니다.

---

## [1.2.1] - 2026-05-27

### Fixed (수정된 버그)
* **활성 프로바이더 수정 저장 비활성화 오류 해결 (v1.2.1 패치):**
  - 기존 연동된 프로바이더에서 Base URL만 변경하려 할 때 API Key 입력이 빈 칸이면 버튼이 비활성화되던 버그를 완벽 해결.
  - 프론트엔드와 백엔드 간에 가상 변경 지시 지침인 `__KEEP_EXISTING__` 안전 암호화 플래그 규격을 체결 및 구현.
  - API Key가 비어 있으면 백엔드는 기존에 로컬 저장소에 보관 중이던 암호화 키(Encrypted API Key, IV)를 고스란히 보존하며 Base URL만 원자적으로 교체 업데이트합니다.

### Added (추가된 기능)
* **⚙️ 수정 모드 전용 안내 가이드 배너 UI 도입:**
  - 이미 활성화된 프로바이더 설정창 진입 시, 상단에 은은한 시안 톤의 가이드 배너(✏️ UI Banner)를 표기하여 API Key를 다시 복사할 필요 없이 Base URL만 수정 가능함을 직관적으로 안내.
  - "수정 저장" 버튼 명칭의 동적 변경을 지원하여 버튼 작동 시나리오와 사용자 힌트 인지율 향상.

---

## [1.2.0] - 2026-05-27

### Fixed (수정된 버그)
* **OpenCode Zen / Go의 공식 Base URL 교정:** 
  - `opencode_zen` 기본 URL을 `https://opencode.ai/zen/v1`로 전격 교정 완료.
  - `opencode_go` 기본 URL을 `https://opencode.ai/zen/go/v1`로 전격 교정 완료.
  - API 키 저장 시 누락되는 경우의 폴백 주소도 공식 주소로 완벽 일치 정정.

### Added (추가된 기능)
* **🔍 모델 탐색기 모달 (Model Explorer Modal) 신규 도입:**
  - 헤더의 select 드롭다운 방식을 파기하고, 모던 돋보기 모양 `[모델 탐색 🔍]` 단추로 개편.
  - 클릭 시 팝업되어 대규모 모델 리스트 속에서 실시간 검색어 필터링(`modelSearchQuery`) 가동.
  - 각 모델 카드 옆에 `[VISION]` 및 `[TEXT]` 배지를 이식해 가독성을 끌어올림.
  - 모델 클릭 또는 더블클릭 시 즉시 대화방에 모델을 로딩(선택) 완료하고 모달을 닫아주는 단축 인터페이스 완성.
* **활성 프로바이더 수정 경로 완전 개방:**
  - 기존의 API 키가 활성화된 상태의 프로바이더일지라도, 사이드바 행을 클릭하면 활성 상태로 변경되는 것과 동시에 자동으로 ⚙️ 설정창 레이어가 팝업되도록 개선.
  - 설정창 구동 시 기존에 등록되었던 Base URL 정보를 폼에 자동 사전 적재(Pre-load)하여 편집 자유도 100% 개방.

---

## [1.1.2] - 2026-05-27

### Added (추가된 기능)
* **단위 테스트(Unit Test) 범위 전격 확장 (DBG-F001 해소):** 
  - `proxy_test.go` 신규 도입: `InjectSystemPrompt` 함수 메시지 가로채기 및 시스템 지침 인젝션 무결성 검증 유닛 테스트 통과.
  - `config_test.go` 신규 도입: `prompts.json` 파일 I/O 및 동일 이름 저장 시 덮어쓰기(편집) 정합성 유닛 테스트 통과.

### Security (보안성 강화)
* **로컬 루프백 격리 바인딩 (SEC-F001 해소):** 외부 기기 및 네트워크(LAN/WAN) 무단 접근 차단을 위해 백엔드 바인딩 주소를 `127.0.0.1` 루프백으로 전격 격리 봉쇄.
* **CORS 엄격 화이트리스트 검증 도입:** `Origin: *` 와일드카드 정책을 영구 파기하고, 사전에 신뢰 수립된 로컬 호스트(`http://localhost:5173`, `http://127.0.0.1:5173`, `http://localhost:8080`, `http://127.0.0.1:8080`) 헤더 요청에 한해서만 CORS 통신을 명시적으로 승인 허가함.

---

## [1.1.1] - 2026-05-27

### Added (추가된 기능)
* **기본 웹 브라우저 자동 기동 (Auto-Open Browser) 지원:** 백엔드 서버 가동 직후 비동기 고루틴을 통해 디바이스 OS별 셸 명령(rundll32/open/xdg-open)을 자동 호출하여 기본 웹 브라우저에서 `http://localhost:8080`을 즉시 띄우도록 가동 최적화.

---

## [1.1.0] - 2026-05-27

### Added (추가된 기능)
* **⚙️ 통합 설정 모달 레이어 수립:** 화면 우측 상단 톱니바퀴(`⚙️`) 버튼을 통해 API Keys 설정 및 Super Prompt 관리 탭 통합 제공.
* **LM Studio & Local LLM 프로바이더 지원:** 기존 3대 프로바이더에 더하여 `lm_studio`, `local_llm` 신규 프로바이더 및 고정 로컬 Base URL 지원 추가.
* **Super Prompt (System Prompt) CRUD 시스템:** `prompts.json` 평문 로컬 데이터베이스를 바탕으로 지침 로딩, 덮어쓰기 편집, 영구 삭제 처리 완료.
* **지능형 system 주입 프록시 중계:** 대화 전송 시 장착된 Super Prompt가 있을 경우 백엔드 프록시 인터셉터 (`InjectSystemPrompt`)가 메시지 0번 인덱스에 system 롤 오브젝트를 가로채어 동적 삽입 후 원격 중계.

---

## [1.0.0] - 2026-05-27

### Added (추가된 기능)
* **AES-256-GCM 로컬 암호화 모듈 (`crypto.go`):** `.secret.key` 자동 인스톨 마스터 파이프라인.
* **CORS 간섭 방지 API Proxy:** 백엔드 단독 통신 중계.
* **동적 Vision 지원 모델 감지 엔진 및 프리미엄 다크 UI 클라이언트.**
