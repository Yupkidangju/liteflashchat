# LiteFlashChat UI/UX 디자인 명세 (designs.md)

본 문서는 LiteFlashChat의 화면 구성, 디자인 테마, UI 컴포넌트 정책 및 상호작용 규칙을 규정하는 디자인 지시서입니다. (v1.4.0 구현 완료 및 Phase 12 구조 정리 로드맵 확정 - 스트리밍, 데이터 관리, 검색, Inspector, 프리셋 UI 수록)

---

## 1. ASCII 기반 설정 레이어 및 v1.4.0 채팅 레이아웃 구조도

```text
+------------------------------------------------------------------------------------------------+
| LiteFlashChat [V 1.4.0]                                             (Settings ⚙️)         |
+------------------------------------------------------------------------------------------------+
| SIDEBAR 290px             | CHAT AREA                                             | PARAMS 240px |
|                           |                                                       |              |
| [+ New Chat]              | [OpenRouter] [Model Explorer: claude-3.5-sonnet]      | Model Params |
|                           |                                                       | Temp [1.0]   |
| [Search chats........]     | assistant/user streaming message stream               | Top P [.95]  |
| - [✎] 첫 질문 제목... [삭제] |                                                       | Top K [40]   |
| - [✎] API 설계 가... [삭제] |                                                       | RP    [1.1]  |
|                           |                                                       | disabled:    |
| * SUPER PROMPT            | 예상 입력 컨텍스트: 12,400 / 198,000 · 출력 2,000     | Inspector    |
| 현재: 번역가 페르소나      | [image] [message input......................] [stop]   | preset/data  |
+------------------------------------------------------------------------------------------------+

============================== [ Settings Setup Layer ] ==============================
| [Tab: 프로바이더 설정] [Tab: Super Prompt 관리] [Tab: 일반 설정] [Tab: 프리셋]    |
| * 프로바이더 설정: Provider / Status / API Key / Base URL / 저장                  |
| * Super Prompt 관리: 이름 / 내용 / 저장 / 목록 [편집] [삭제]                      |
| * 일반 설정: 컨텍스트 압축 비율 / 모델 메타데이터 보정 / 데이터 관리              |
| * 프리셋: 현재 설정 저장 / 적용 / 이름 변경 / 삭제                               |
====================================================================================
```

---

## 2. 각 부분별 기능 상세 설명

### A. 우측 상단 설정(⚙️) 레이어 통합
* **진입점:** 사이드바의 프로바이더명/Edit/활성상태 목록을 제거하고, 화면 우측 상단 헤더의 `⚙️` 버튼으로 프로바이더 설정과 상태 확인을 단일화합니다.
* **레이아웃:** 탭 스위처(Tab Switcher)를 도입합니다.
  - **프로바이더 설정 탭:** OpenRouter, OpenCode Zen, OpenCode Go, LM Studio, Local LLM의 API Key, Base URL, 활성 상태, 오류 메시지를 한 화면에서 확인하고 수정합니다.
  - **Super Prompt 관리 탭:** Super Prompt 생성, 편집, 삭제만 수행하며 대화방 장착 기능은 제공하지 않습니다.

### B. Super Prompt CRUD 인터페이스 디자인
* **입력 영역:**
  - **Prompt Name 필드:** 텍스트 인풋 (`placeholder="프롬프트 이름 입력..."`).
  - **Prompt Content 필드:** 텍스트 에어리어 (`rows={4}`, `placeholder="시스템 지침 입력..."`).
  - **[저장] 버튼:** 입력한 프롬프트를 저장합니다. 백엔드 `/api/prompts` POST API를 호출합니다.
* **목록 영역:**
  - 저장 완료된 프롬프트 목록이 하단 테이블/리스트에 나열됩니다.
  - 각 행 우측에 **[가져오기]** 및 **[삭제]** 버튼 배치.
  - **[가져오기] 버튼 클릭 시:** 선택된 프롬프트 내용이 상단 입력 영역의 이름과 내용 필드에 그대로 로드(복원)됩니다. 이 상태에서 내용을 수정하고 `[저장]`을 다시 누르면, 동일한 이름으로 백엔드 저장소가 감지하여 자연스럽게 덮어쓰기(Overwrite) 편집이 성립됩니다.
  - **[삭제] 버튼 클릭 시:** 해당 프롬프트를 영구히 삭제합니다. 백엔드 `/api/prompts` DELETE API를 무비하게 연계 호출합니다.
* **활성화 표시:** 현재 대화방에서 어떤 Super Prompt가 장착되었는지는 왼쪽 사이드바의 전용 Super Prompt 패널에 `현재: <이름>` 형태로 표시됩니다.

### C. Super Prompt 장착 전용 사이드바 디자인
* **목적:** 설정 모달의 CRUD와 대화방 장착을 분리하여 사용자가 프롬프트를 수정하는 행동과 현재 대화방에 적용하는 행동을 혼동하지 않게 합니다.
* **구성:** 현재 장착명, 저장된 프롬프트 선택 박스, `[적용]`, `[해제]`, `[관리]` 버튼으로 구성합니다.
* **제약:** 편집과 추가/삭제는 설정 모달에서만 가능하며, 사이드바에서는 현재 대화방에 대한 적용 상태만 바꿀 수 있습니다.

### D. 대화 제목 편집 디자인 (v1.3.0)
* **목적:** 모든 대화방이 `새로운 대화`로 남는 문제를 제거하고, 사용자가 대화 목록을 재방문할 때 첫 질문 또는 수동 제목으로 식별할 수 있게 합니다.
* **자동 제목:** 새 대화방은 `isTitleAutoGenerated=true`로 생성되며, 첫 사용자 메시지 발송 시 메시지 앞부분 최대 32자를 제목으로 저장합니다.
* **수동 제목:** 대화 목록 항목의 `✎` 버튼 또는 제목 더블클릭으로 인라인 입력 상태에 진입합니다. 저장 시 `isTitleAutoGenerated=false`로 바꾸어 이후 자동 갱신을 차단합니다.
* **빈 제목 방지:** 사용자가 공백만 입력하면 저장하지 않고 이전 제목을 유지합니다.

### E. 우측 모델 파라미터 패널 (v1.3.0)
* **위치:** 채팅 영역 오른쪽에 폭 240px 고정 패널을 둡니다. 화면 폭이 좁으면 헤더 버튼으로 열리는 슬라이드 패널로 전환합니다.
* **컨트롤:** `temperature`, `top_p`, `top_k`, `repetition_penalty`를 슬라이더와 숫자 입력으로 제공합니다.
* **활성화 규칙:** 선택 모델의 `supportedParameters`에 해당 키가 있을 때만 활성화합니다. 불명확한 항목은 비활성화하고 "모델 메타데이터에서 지원 확인 불가" 문구를 표시합니다.
* **값 범위와 기본값:** `temperature 0.0~2.0` 기본 `1`, `top_p 0.0~1.0` 기본 `0.95`, `top_k 0 이상 정수` 기본 `40`, `repetition_penalty 0.0~2.0` 기본 `1.1`로 고정합니다.
* **저장:** 값은 현재 `ChatSession.generationSettings`에 저장하고 새 대화는 앱 기본값을 복사합니다.
* **메타데이터 표시:** 패널 하단에는 `Context`, `Input`, `Output`, `Source`를 표시합니다. 값이 없으면 `알 수 없음`, `isContextEstimated=true`이면 `수동 보정 필요`를 표시합니다.

### F. 컨텍스트 사용량 바 및 압축 설정 (v1.3.0)
* **위치:** 채팅 입력창 바로 위, 첨부 미리보기 아래에 한 줄 컨텍스트 바를 표시합니다.
* **표시 형식:** `예상 입력 컨텍스트: {used} / {maxInputTokens 또는 알 수 없음} tokens · 압축 기준 {compressionRatio * 100}% · 출력 {maxOutputTokens 또는 알 수 없음}`.
* **상태 색상:** 70% 미만은 보조 텍스트, 70% 이상은 시안 강조, 90% 이상은 경고 레드로 표시합니다.
* **설정 위치:** 설정 모달에 `일반 설정` 탭을 추가하고 `컨텍스트 압축 비율` 입력을 제공합니다. 기본값은 `0.7`입니다.
* **압축 동작:** 압축 트리거를 넘으면 사용자가 보낸 메시지 전송 전에 LLM 요약을 수행하며, 실패 시 전송을 중단하고 오류를 표시합니다.
* **수동 보정:** 모델 메타데이터가 불명확하면 일반 설정 탭에서 전체 컨텍스트, 입력 한도, 출력 한도를 수동 저장합니다.

### G. 스트리밍 응답 및 요청 취소 UI (v1.4.0)
* **기본 동작:** 사용자가 `[전송]`을 누르면 assistant 말풍선을 즉시 생성하고 `streaming` 상태로 표시합니다.
* **점진 표시:** 수신된 delta 텍스트는 같은 assistant 말풍선의 본문 끝에 누적합니다. 별도 임시 말풍선을 만들지 않습니다.
* **전송 버튼 전환:** 스트리밍 중에는 `[전송]` 버튼이 `[중지]`로 바뀌며, 클릭 시 요청을 취소하고 현재까지 받은 응답을 보존합니다.
* **상태 표시:** 완료는 별도 배지 없이 일반 말풍선으로 표시하고, 취소는 `중지됨`, 오류는 `오류` 보조 라벨을 말풍선 하단에 표시합니다.
* **잠금 정책:** 스트리밍 중에는 모델 변경, provider 변경, 프리셋 적용, JSON 가져오기를 비활성화합니다.

### H. 대화 검색 UI (v1.4.0)
* **위치:** 좌측 사이드바 `Chat History` 제목 바로 아래에 검색 입력을 배치합니다.
* **표시 형식:** 검색어가 있으면 `검색 결과 {n}개`를 보조 텍스트로 표시합니다.
* **필터 기준:** 대화 제목, 모든 메시지 본문, provider, model, 장착된 Super Prompt 이름을 소문자 기준으로 비교합니다.
* **빈 상태:** 검색 결과가 없으면 대화 목록 자리에 `검색 결과가 없습니다.`를 표시합니다.

### I. 데이터 관리 UI (v1.4.0)
* **위치:** 설정 모달 `일반 설정` 탭 하단에 `데이터 관리` 섹션을 추가합니다.
* **버튼:** `[JSON 내보내기]`, `[JSON 가져오기]`, `[Markdown 내보내기]` 세 버튼만 제공합니다.
* **가져오기 피드백:** 성공 시 병합된 대화/프리셋/보정값 개수를 alert 또는 상태문구로 표시합니다. 실패 시 원인 문구를 표시하고 기존 데이터는 변경하지 않습니다.
* **보안 안내:** 내보내기 섹션에는 API Key와 secret은 포함되지 않는다는 짧은 보조 문구를 표시합니다.

### J. Inspector 패널 (v1.4.0)
* **위치:** 우측 패널에서 `Model Params` 아래에 접을 수 있는 `Inspector` 섹션을 배치합니다.
* **표시 데이터:** endpoint, provider, model, streaming 여부, response status, sanitized request, response preview, context summary 사용 여부를 표시합니다.
* **복사 버튼:** `[요청 복사]`, `[응답 복사]` 버튼을 제공하며, 복사 대상에는 Authorization/API Key/암호문이 포함되면 안 됩니다.
* **빈 상태:** 아직 요청이 없으면 `아직 기록된 요청이 없습니다.`를 표시합니다.

### K. 프리셋 UI (v1.4.0)
* **위치:** 설정 모달에 `프리셋` 탭을 추가합니다.
* **저장 폼:** 프리셋 이름 입력과 `[현재 설정으로 저장]` 버튼을 제공합니다.
* **목록 액션:** 각 프리셋 행에 `[적용]`, `[이름 변경]`, `[삭제]`를 제공합니다.
* **적용 실패:** provider가 설정되지 않았거나 모델 목록에서 대상 모델을 찾을 수 없으면 적용하지 않고 오류 문구를 표시합니다.

### L. Phase 12 UI 컴포넌트 분리 기준 (구현 완료)
* **목적:** 시각 디자인과 사용자 동작을 변경하지 않고 `src/App.tsx`의 JSX 책임을 기능별 컴포넌트로 분리합니다.
* **컴포넌트 경계:**
  - `Sidebar`: 새 대화, 검색, 대화 목록, 제목 편집, Super Prompt 장착 패널, footer 표시.
  - `ChatHeader`: provider/model 표시, 모델 탐색기 열기, 설정 열기, 오류 표시.
  - `MessageList`: 메시지 말풍선, 첨부 이미지, streaming/cancelled/error 상태 라벨.
  - `InputPanel`: 첨부, 컨텍스트 바, 텍스트 입력, 전송/중지 버튼.
  - `SettingsModal`: 프로바이더 설정, Super Prompt 관리, 일반 설정, 데이터 관리, 프리셋 탭.
  - `ModelSearchModal`: 모델 검색, Vision/Text 배지, 모델 선택.
  - `ParamsPanel`: 모델 파라미터, 메타데이터, Inspector 표시와 복사 액션.
* **구현 상태:** 위 컴포넌트는 `src/components/*`에 분리되었고, `src/App.tsx`는 862줄로 축소되었습니다. 스타일은 `src/styles/*`로 분리하고 `src/index.css`에는 전역 토큰과 import만 유지합니다.
* **금지 사항:** Phase 12 컴포넌트 분리는 레이아웃 크기, CTA 명칭, 버튼 활성화 조건, 색상 토큰을 변경하지 않습니다.

---

## 3. UI 요소 및 버튼 정책 (CTA Policies)

### 3.1 톱니바퀴 버튼 `⚙️`
* **활성화 조건:** 상시 활성화 (`z-index: 50`).
* **동작:** 누르면 반투명 설정 레이어 활성화.

### 3.2 프롬프트 [가져오기] 버튼
* **동작:** 해당 행의 `name`과 `content`를 React 입력 Form State인 `inputPromptName`, `inputPromptContent`에 복제 대입하며, 모달 창은 닫지 않고 입력 포커스를 해당 폼에 동기화합니다.

### 3.3 프롬프트 [저장] 버튼
* **비활성화 조건:** 이름 혹은 내용 필드가 비어있을 때 비활성화(`disabled`).
* **동작:** 백엔드로 저장 완료 신호를 보낸 뒤, 프론트 목록을 갱신하고 입력 폼을 깨끗이 초기화(Reset)합니다.

### 3.4 프로바이더 [암호화 저장 완료 / 수정 저장] 버튼 (v1.2.1 신규)
* **신규 등록 모드:** API Key 입력란이 비어 있으면 비활성화(`disabled`). "암호화 저장 완료" 문구 노출.
* **수정 편집 모드:** API Key와 Base URL이 모두 기존 상태와 정확히 동일하면 비활성화(`disabled`). API Key를 새로 타이핑하거나 Base URL을 기존과 다르게 1글자라도 변경할 경우 즉시 활성화(`enabled`). "수정 저장" 문구 노출.
* **동작:** API Key가 비어 있으면 백엔드에 `__KEEP_EXISTING__` 플래그를 실어 보내 기존 안전 키를 유지하고 Base URL만 갱신하며, 성공 시 모달을 닫지 않고 상태를 실시간 갱신(Refetch Providers)합니다.

### 3.5 프로바이더 저장 즉시 반영 정책 (v1.2.4 신규)
* **동작:** 저장 성공 후 최신 프로바이더 목록을 재조회하고 저장 대상 프로바이더를 활성 프로바이더와 현재 대화방 provider에 동기화합니다.
* **모델 갱신:** 저장된 프로바이더가 활성 상태이면 모델 목록을 즉시 재조회하며, 실패하면 헤더 오류 영역에 원인 문구를 표시합니다.
* **상태 위치:** 프로바이더 활성/비활성 표시와 상태 메시지는 설정 모달 내부 카드에서 확인합니다.

### 3.6 대화 제목 [편집] CTA (v1.3.0)
* **활성화 조건:** 대화 목록에 세션이 존재할 때 상시 활성화합니다.
* **동작:** 인라인 입력 필드를 표시하고 Enter 또는 포커스 아웃으로 저장합니다. Escape는 취소합니다.
* **후속 상태:** `ChatSession.title` 갱신, `isTitleAutoGenerated=false`, `litechat_sessions` 즉시 저장.

### 3.7 모델 파라미터 컨트롤 CTA (v1.3.0)
* **활성화 조건:** 선택 모델의 지원 플래그가 true인 항목만 활성화합니다.
* **동작:** 입력 즉시 현재 세션의 `generationSettings`를 갱신하되, 실제 원격 전송은 다음 `/api/chat` 호출 시 수행합니다.
* **비활성화 조건:** `supportedParameters`에 항목이 없거나 모델이 선택되지 않은 경우 disabled 상태로 유지합니다.

### 3.8 컨텍스트 압축 비율 CTA (v1.3.0)
* **활성화 조건:** 설정 모달 일반 설정 탭에서 상시 활성화합니다.
* **동작:** `0.1~1.0` 범위 값만 저장하며, 기본값 `0.7`을 `localStorage`에 보존합니다.
* **후속 상태:** 모든 새 대화방의 `compressionRatio` 기본값에 반영합니다.

### 3.9 [전송]/[중지] CTA (v1.4.0)
* **전송 활성화 조건:** 입력 텍스트가 있거나 첨부가 있고, 모델이 선택되어 있으며, 현재 스트리밍 중이 아닐 때 활성화합니다.
* **중지 활성화 조건:** 현재 assistant 메시지가 `streaming` 상태일 때 활성화합니다.
* **후속 상태:** 중지 시 `AbortController.abort()`를 호출하고 assistant 메시지를 `cancelled`로 저장합니다.

### 3.10 데이터 관리 CTA (v1.4.0)
* **JSON 내보내기:** 현재 세션/프리셋/모델 보정값을 `liteflashchat-backup-{YYYYMMDD-HHmmss}.json`으로 다운로드합니다.
* **JSON 가져오기:** 선택 파일 검증 후 기존 데이터를 삭제하지 않고 병합합니다.
* **Markdown 내보내기:** 현재 대화 또는 전체 대화를 `liteflashchat-chat-{YYYYMMDD-HHmmss}.md`로 다운로드합니다.

### 3.11 Inspector 복사 CTA (v1.4.0)
* **활성화 조건:** 마지막 요청 snapshot이 있을 때 활성화합니다.
* **동작:** sanitized request 또는 response preview를 클립보드에 복사합니다.
* **실패 처리:** 브라우저 클립보드 권한이 없으면 텍스트 선택 가능한 영역을 유지하고 오류 문구를 표시합니다.

### 3.12 프리셋 CTA (v1.4.0)
* **저장:** provider와 model이 선택되어 있고 이름이 공백이 아닐 때 활성화합니다.
* **적용:** 현재 스트리밍 중이 아니고 대상 provider/model을 사용할 수 있을 때만 활성화합니다.
* **삭제:** 확인창 승인 후 해당 프리셋만 제거합니다.

---

## 4. 디자인 시스템 토큰 (Designs Tokens)
* **Glass Container BG:** `rgba(17, 24, 39, 0.75)` (Glassmorphism 오버레이)
* **Accent gradient:** `linear-gradient(135deg, #8B5CF6, #06B6D4)`
* **Modal Dialog BG:** `#111827` (안정적인 다크그레이 솔리드 박스)
* **Modal Text Main:** `#F8FAFC` (설정 모달 본문 및 라벨 고대비 텍스트)
* **Modal Text Muted:** `#CBD5E1` / `#AEB7C6` (4.5:1 대비 목표의 보조 텍스트)

---

## 5. [v1.2.0 신규] 모델 탐색기 모달 (Model Explorer Modal) 인터페이스 디자인

### A. 진입점 및 트리거 버튼
* **위치:** 우측 상단 헤더 영역의 모델 선택 셀렉트박스를 제거하고, 그 위치에 `[모델 탐색 🔍 : <현재모델명>]` 버튼을 배치합니다.
* **디자인:** 반투명 글래스 테두리에 호버 시 테두리 색상이 Electric Cyan으로 은은하게 발광하는 프리미엄 미학 단추입니다.

### B. 모달 구조도 (ASCII)
```text
=================== [ Model Explorer 🔍 ] ===================
|                                                           |
|  [ 검색: 모델 ID 또는 키워드 입력...               ]       |
|  -------------------------------------------------------  |
|  * 사용 가능한 AI 모델 리스트                             |
|  +-----------------------------------------------------+  |
|  | [Vision] anthropic/claude-3.5-sonnet                |  |
|  | Context: 200,000 | Vision Enabled           [Select]|  |
|  +-----------------------------------------------------+  |
|  | [Text]   meta-llama/llama-3-8b-instruct             |  |
|  | Context: 8,192   | Text Only                [Select]|  |
|  +-----------------------------------------------------+  |
|                                                           |
|                                                [ Close X ]|
=============================================================
```

### C. 검색 및 필터 인터랙션
* **실시간 검색 (Reactive Search):** 사용자가 검색창에 문자를 타이핑하는 즉시(100ms 이내) 검색어와 일치하지 않는 모델 카드는 화면에서 페이드아웃(Fade-out) 처리되며 필터링됩니다.
* **비전 꼬표(Vision/Text 배지):** 이미지 업로드를 수용하는 모델 옆에는 푸른색 `[Vision]` 배지가 선명하게 인퓨전되고, 일반 텍스트 모델 옆에는 그레이 톤의 `[Text Only]` 배지가 달려 탐색성을 극대화합니다.
* **더블클릭 단축 지원:** 모델 카드 행을 더블클릭(`onDoubleClick`) 시 별도의 [선택 완료] 클릭 없이 즉시 대화방에 모델을 로딩하고 모달 창을 닫아줍니다.

---

## 6. [v1.2.1 신규] 프로바이더 수정 안내 가이드 배너 UI
* **노출 조건:** `isProviderEditMode`가 `true`일 때 (이미 Key가 등록되어 활성화된 프로바이더를 ⚙️ 모달창에서 편집하고 있을 때)
* **스타일 토큰:**
  - **배경색 (Background):** `rgba(6, 182, 212, 0.08)` (은은한 일렉트릭 시안 투명 배경)
  - **테두리 (Border):** `1px solid rgba(6, 182, 212, 0.25)` (부드러운 시안 테두리)
  - **폰트 색상 (Color):** `var(--color-accent-end)` (Cyan, 가독성 확보)
  - **패딩 (Padding):** `10px 14px`
  - **보더 라디우스 (Border Radius):** `6px`
  - **자간/줄높이 (Line Height):** `1.5`
* **안내 문구:** "✏️ 이 프로바이더는 이미 활성화되어 있습니다. API Key를 비워두면 기존 키가 유지됩니다. Base URL이나 API Key를 변경한 후 "수정 저장"을 클릭하세요."
* **UX 가치:** 기존 등록 정보를 편집하려는 사용자에게 API Key 재입력 의무가 없음을 명시적으로 인지시켜 입력 피로도를 0으로 수렴시킵니다.
