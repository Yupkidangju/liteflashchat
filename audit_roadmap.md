# 개발 로드맵 및 통합 감사 프레임워크 (audit_roadmap.md)

본 문서는 LiteFlashChat 개발 마일스톤과 각 Phase별 완료 정합성을 검증하기 위한 감사 리포트 설계도입니다. (v1.4.0 구현 완료 및 Phase 12 구조 정리 로드맵 확정 - Phase 11: 스트리밍/API 플랫폼 기능 감사 기준 수록)

---

## 1. 4단계 통합 감사 프로세스 (Audit Frame)

### 1.1 정합성 감사 (Consistency Audit)
* **목표:** 스펙 문서(`spec.md`)의 API 명세, 설계 명세(`designs.md`)의 인터랙션 정책 및 소스코드가 일관성을 유지하는가?
* **핵심 체크포인트:** 사이드바 Super Prompt 패널에서 프롬프트를 장착했을 때, 해당 데이터가 대화 발송 시 `/api/chat` 프록시 메시지의 최상단 `system` 롤에 올바르게 이식되는지 검사.
* **v1.2.2 추가 체크포인트:** `/api/providers`의 `configStatus`와 UI의 모델 로드 오류 표시가 실제 키 상태와 일치하는지 검사.
* **v1.2.4 추가 체크포인트:** 프로바이더 저장 직후 새로고침 없이 활성 프로바이더, 현재 대화방 provider, 모델 목록이 갱신되는지 검사.
* **v1.3.0 추가 체크포인트:** `/api/models`의 `ModelInfo` 확장 필드가 모델 탐색기, 우측 패널, 컨텍스트 바, `/api/chat` payload에 동일하게 반영되는지 검사.
* **v1.3.1 추가 체크포인트:** 명시 컨텍스트가 없는 OpenAI 호환 모델이 임의 `8192`로 표시되지 않고, LM Studio `/api/v0/models.max_context_length` 또는 수동 보정값만 확정값으로 적용되는지 검사.
* **v1.4.0 추가 체크포인트:** `/api/chat/stream`, `ChatMessage.status`, `ChatExportBundle`, `InspectorSnapshot`, `ChatPreset`이 `spec.md`와 UI 동작에서 같은 이름과 의미로 연결되는지 검사.

### 1.2 위험요소 감사 (Risk Audit)
* **목표:** 보안 취약성(암호화 키 탈취, API 키 평문 누출) 및 런타임 오류가 해결되었는가?
* **핵심 체크포인트:** 평문 프롬프트 저장소(`prompts.json`)에 API 키와 같은 중요 기밀이 잘못 유입되지 않도록 UI 입력창 경계 분리 점검.
* **v1.2.2 추가 체크포인트:** `go test`가 실제 `.secret.key`를 삭제하지 않는지, `__KEEP_EXISTING__` 저장이 깨진 암호문을 유지하지 않는지 검사.
* **v1.2.4 추가 체크포인트:** 설정 모달의 Super Prompt 관리 기능이 대화방 장착을 직접 수행하지 않고, 장착/해제는 사이드바 전용 컨트롤로만 수행되는지 검사.
* **v1.3.0 추가 체크포인트:** 지원 여부가 명시되지 않은 모델 파라미터가 UI에서 비활성화되고 payload에 포함되지 않는지 검사.
* **v1.3.1 추가 체크포인트:** 기본 생성 파라미터가 `temperature=1`, `top_p=0.95`, `top_k=40`, `repetition_penalty=1.1`로 초기화되며, 지원되지 않는 파라미터는 여전히 payload에서 제외되는지 검사.
* **v1.4.0 추가 체크포인트:** Inspector, JSON 내보내기, Markdown 내보내기, stream 이벤트 어디에도 API Key, Authorization, `.secret.key`, `keys.json` 암호문이 노출되지 않는지 검사.

### 1.3 아키텍처 감사 (Architecture Audit)
* **목표:** 백엔드 Go net/http 경량 구조가 유지되고 있으며, 프론트엔드가 불필요한 전역 상태관리 라이브러리 없이 온전히 동작하는가?
* **v1.3.0 추가 체크포인트:** 컨텍스트 압축이 별도 상태 라이브러리나 외부 DB 없이 `ChatSession`과 백엔드 요약 API 계약으로만 닫히는지 검사.
* **v1.3.1 추가 체크포인트:** 수동 모델 한도 보정이 외부 DB 없이 provider/model별 `localStorage` 보정 맵과 `/api/models` 재수화 흐름으로만 닫히는지 검사.
* **v1.4.0 추가 체크포인트:** 스트리밍, 검색, 백업, Inspector, 프리셋이 외부 DB 없이 Go 프록시와 React/localStorage 경계 안에서 닫히는지 검사.

### 1.4 로드맵 감사 (Roadmap Audit)
* **목표:** Phase 9~11 구현 항목이 `spec.md` 타입, `designs.md` CTA, `implementation_summary.md` 파일 책임과 같은 이름으로 연결되어 있는지 검사합니다.
* **완료 기준:** 문서에 등장하는 `ModelInfo`, `GenerationSettings`, `ModelMetadataOverride`, `ContextCompressionSettings`, `ChatExportBundle`, `InspectorSnapshot`, `ChatPreset`, `isTitleAutoGenerated`, `contextSummary`가 구현 대상 타입과 테스트 기준에 모두 존재해야 합니다.
* **Phase 12 감사 기준:** `src/App.tsx`, `src/index.css`, `main.go`, `proxy.go`의 책임 분리 로드맵이 기능 회귀 없이 진행되는지 검사합니다.

---

## 2. 단계별 마일스톤 로드맵 (Milestones Roadmap)

### 2.1 Phase 1: 백엔드 코어 및 암호화 기틀 확립
* **목표:** `.secret.key` 자동 인스톨 및 `keys.json` AES-256-GCM 암복호화 파이프라인 완성.

### 2.2 Phase 2: API 라우터 구축 및 프록시 중계 완성
* **목표:** `/api/keys`, `/api/models`, `/api/chat` 프록시 서버 연동 완료.

### 2.3 Phase 3: 프론트엔드 프리미엄 UI 조립 및 동적 바인딩
* **목표:** React 컴포넌트 마운팅 및 다크 테마 Glassmorphism UI 구현 완료.

### 2.4 Phase 4: 대화 챗 기능 완성 및 파일 피드백 검증
* **목표:** 텍스트/이미지 대화 실송수신 및 로컬 아카이빙 통합.

### 2.5 Phase 5: Super Prompt CRUD 및 ⚙️ 설정 레이어 연동
* **목표:** ⚙️ 우측 상단 오버레이 레이어로 설정 창 스위칭, 5대 프로바이더 전면 지원, Super Prompt 로컬 저장(prompts.json) 및 채팅 시 system 롤 주입 완성.
* **구현 항목:**
  - `config.go` 내의 `prompts.json` 파일 I/O 구현 및 `main.go` 에 `/api/prompts` 엔드포인트 개설.
  - `proxy.go` 내에 `system` 프롬프트 인터셉트 주입 모듈(`InjectSystemPrompt`) 이식.
  - 프론트엔드 `App.tsx` 우측 상단 `⚙️` 버튼 배치 및 탭 전환형(프로바이더 설정 / Super Prompt 관리) 통합 설정 모달 UI 구현.
  - Super Prompt 리스트 로딩, 덮어쓰기 편집, 삭제 연동 완성.
* **검증 포인트:**
  - `curl -X POST -H "Content-Type: application/json" -d '{"name":"번역기","content":"당신은 번역가입니다."}' http://localhost:8080/api/prompts` 호출 후 프로젝트 루트에 평문 `prompts.json`이 미려하게 생성되었는지 확인.
  - Super Prompt 활성 상태로 질문 발송 시, 백엔드 콘솔 또는 디버그 덤프에 `{role: "system", content: "..."}`이 0번 인덱스 메시지에 부착되어 흘러가는지 패킷/데이터 덤프 감사.

### 2.6 Phase 6: 프로바이더 설정 순환성 안정화 (v1.2.2 완료)
* **목표:** 프로바이더 설정, 키 저장, 모델 조회, 모델 선택, 채팅 발송, 설정 수정 흐름이 암호키 손상이나 URL 오입력으로 막히지 않도록 안정화합니다.
* **구현 단위:**
  - **Unit 6-1 키 보존 테스트 격리:** `crypto_test.go`가 테스트 전용 임시 `.secret.key`만 사용하도록 변경하고 실제 사용자 키 삭제 경로 제거.
  - **Unit 6-2 프로바이더 메타 계약 통합:** `providers.go`를 추가하여 5대 프로바이더 식별자, 기본 URL, 로컬 여부, URL 정규화 규칙을 단일 소스로 통합.
  - **Unit 6-3 Base URL 정규화:** `/models`, `/chat/completions`가 포함된 입력값을 API 루트로 저장하고, 잘못된 스킴과 미지원 provider를 거부.
  - **Unit 6-4 기존 키 유지 가드:** `__KEEP_EXISTING__` 저장 전 기존 암호문 복호화 검증을 수행하고 실패 시 재등록 요구.
  - **Unit 6-5 로컬 프로바이더 무키 저장:** `lm_studio`, `local_llm`은 API Key 없이 Base URL만 저장해도 내부 더미 토큰으로 모델 조회 흐름 유지.
  - **Unit 6-6 프론트 오류 진단 UX:** 모델 로드 실패 원인을 화면과 alert에 표시하고, 저장 성공 후 현재 프로바이더의 모델 목록을 즉시 재조회.
  - **Unit 6-7 품질 게이트 복구:** `npm run lint`, `npm run build`, `go test ./...`, `go build -buildvcs=false .` 통과.
* **검증 포인트:**
  - `go test -v ./...` 실행 전후 실제 `.secret.key`가 삭제되지 않아야 합니다.
  - `normalizeProviderBaseURL("opencode_go", ".../v1/chat/completions")` 결과는 `.../v1`이어야 합니다.
  - 깨진 암호문 상태에서 `__KEEP_EXISTING__` 저장은 HTTP 409로 실패해야 합니다.
  - 로컬 LLM 프로바이더는 API Key 입력 없이도 설정 저장 버튼이 활성화되어야 합니다.

### 2.7 Phase 7: 범용 output 패키징 빌드 (v1.2.3 완료)
* **목표:** 운영체제별 shell 차이에 의존하지 않고 동일한 npm 명령으로 실행 파일과 정적 웹 자산을 `output/`에 생성합니다.
* **구현 단위:**
  - **Unit 7-1 Node 패키징 스크립트:** `scripts/build-output.mjs`에서 `npm run build`, `go build -buildvcs=false`, `dist` 복사를 순차 실행.
  - **Unit 7-2 OS별 실행 파일명 처리:** Windows는 `liteflashchat.exe`, Linux/macOS는 `liteflashchat`로 출력.
  - **Unit 7-3 산출물 격리:** `output/`을 Git 제외 대상으로 추가하고 로컬 비밀 파일은 복사 대상에서 제외.
  - **Unit 7-4 문서화:** `BUILD_GUIDE.md`, `README.md`, `CHANGELOG.md`, `spec.md`에 빌드 명령과 출력 구조 반영.
* **검증 포인트:**
  - `npm run build:output` 실행 후 `output/dist/index.html`이 존재해야 합니다.
  - 현재 운영체제용 실행 파일이 `output/` 바로 아래에 존재해야 합니다.
  - `output/` 안에 `.secret.key`, `keys.json`이 자동 복사되지 않아야 합니다.

### 2.8 Phase 8: 설정 UX 즉시 반영 및 책임 분리 (v1.2.4 완료)
* **목표:** 웹 인터페이스에서 프로바이더 저장 후 새로고침이 필요한 병목을 제거하고, 프로바이더 설정과 Super Prompt 적용 흐름을 분리합니다.
* **구현 단위:**
  - **Unit 8-1 Provider Refetch 반환 계약:** `fetchProviders()`가 최신 provider 배열을 반환하여 저장 후 호출자가 즉시 후속 상태 반영을 수행할 수 있게 변경.
  - **Unit 8-2 저장 후 활성 상태 동기화:** 저장 대상 프로바이더를 `activeProvider`와 현재 대화방 `provider`에 반영하고, 모델 목록을 즉시 재조회.
  - **Unit 8-3 사이드바 프로바이더 목록 제거:** 좌측 하단 provider/Edit/LED 목록을 제거하고 상태 확인을 설정 모달 상태 카드로 단일화.
  - **Unit 8-4 Super Prompt 책임 분리:** 설정 모달은 생성/편집/삭제만 담당하고, 사이드바는 선택/적용/해제만 담당.
  - **Unit 8-5 명도 대비 개선:** 설정 모달 라벨, 입력 필드, 목록, 상태 카드 색상을 고대비 토큰으로 조정.
* **검증 포인트:**
  - `npm run lint`와 `npm run build`가 통과해야 합니다.
  - 설정 저장 성공 후 브라우저 새로고침 없이 모델 목록 재조회가 실행되어야 합니다.
  - 설정 모달 Super Prompt 목록에는 `[편집]`, `[삭제]`만 존재하고, 대화방 적용 버튼은 사이드바에만 있어야 합니다.
  - 설정 모달의 보조 텍스트가 어두운 배경에서 읽기 쉬운 명도 대비를 가져야 합니다.

### 2.9 Phase 9: 모델 메타데이터/컨텍스트 관리 고도화 (v1.3.0 구현 완료)
* **목표:** 모델 선택 시 모델별 최대 컨텍스트, 출력 한도, 지원 파라미터를 적용하고, 대화 제목 관리와 컨텍스트 압축 흐름을 사용자 제어 가능한 UI로 완성합니다.
* **구현 단위:**
  - **Unit 9-1 모델 메타데이터 계약 확장:** `UIModelInfo`와 `ModelInfo`에 `maxInputTokens`, `maxOutputTokens`, `supportedParameters`, 지원 플래그를 추가합니다.
  - **Unit 9-2 모델 파서 구현:** OpenRouter의 `context_length`, `top_provider.max_completion_tokens`, `supported_parameters`를 파싱하고, OpenAI 호환 응답은 명시 필드만 반영합니다.
  - **Unit 9-3 세션 저장 마이그레이션:** 기존 `litechat_sessions`를 읽을 때 누락된 `isTitleAutoGenerated`, `generationSettings`, `compressionRatio`, `contextSummary`, `summarizedMessageIds`를 기본값으로 보정합니다.
  - **Unit 9-4 대화 제목 자동/수동 편집:** 첫 사용자 메시지로 자동 제목을 저장하고, `✎` 편집으로 수동 제목을 저장하면 자동 갱신을 차단합니다.
  - **Unit 9-5 우측 모델 파라미터 패널:** `temperature`, `top_p`, `top_k`, `repetition_penalty` 컨트롤을 추가하고, 모델 메타데이터가 지원을 명시한 항목만 활성화합니다.
  - **Unit 9-6 컨텍스트 사용량 바:** 입력창 바로 위에 `예상 사용량 / 최대 컨텍스트 / 압축 기준`을 표시하고 70%, 90% 임계 색상을 적용합니다.
  - **Unit 9-7 설정 모달 일반 설정 탭:** 컨텍스트 압축 비율 기본값 `0.7`을 저장하고 `0.1~1.0` 범위만 허용합니다.
  - **Unit 9-8 LLM 요약 압축:** v1.3.1 기준 `estimatedPromptTokens > maxInputTokens * compressionRatio`이면 `/api/chat/summary`를 먼저 호출하고 성공한 요약만 세션에 저장합니다.
  - **Unit 9-9 채팅 payload 필터링:** 지원되는 샘플링 파라미터와 확정 출력 한도만 `/api/chat` 원격 payload에 포함합니다.
  - **Unit 9-10 문서/패키징 동기화:** 구현 완료 후 `spec.md`, `designs.md`, `README.md`, `CHANGELOG.md`, `DESIGN_DECISIONS.md`, `implementation_summary.md`, `BUILD_GUIDE.md`, `LESSONS_LEARNED.md`, `audit_roadmap.md`를 실제 구현 결과로 갱신합니다.
* **실제 알고리즘 메모:**
  - 컨텍스트 예상 토큰은 v1.3.1에서 `Math.ceil(text.length / 4)`로 통일합니다.
  - 압축 트리거는 `maxInputTokens * compressionRatio`이며 기본값은 `0.7`입니다.
  - 요약 실패 시 오래된 메시지를 임의 삭제하지 않고 사용자 메시지 전송을 중단합니다.
* **검증 포인트:**
  - OpenRouter 모델 fixture에서 `maxOutputTokens`와 `supportedParameters`가 채워져야 합니다.
  - `supportedParameters`가 빈 모델은 우측 패널의 네 파라미터가 모두 비활성화되어야 합니다.
  - 첫 메시지 발송 후 대화 제목이 첫 문장으로 바뀌고, 수동 변경 후에는 새 메시지로 덮어쓰이지 않아야 합니다.
  - 컨텍스트 초과 시 요약 API가 먼저 호출되고, 실패하면 일반 채팅 API가 호출되지 않아야 합니다.

### 2.10 Phase 10: 모델 컨텍스트 보정 및 기본 파라미터 정정 (v1.3.1 구현 완료)
* **목표:** 사용자가 지적한 컨텍스트/출력 한도 혼동을 제거하고, 모델이 실제 제공하는 전체/입력/출력 컨텍스트만 확정값으로 적용합니다.
* **구현 단위:**
  - **Unit 10-1 임의 8K 폴백 제거:** OpenAI 호환 모델 목록에 컨텍스트 필드가 없으면 `contextLength=0`, `maxInputTokens=0`, `metadataSource=unknown`, `isContextEstimated=true`로 반환합니다.
  - **Unit 10-2 LM Studio v0 메타데이터 병합:** `lm_studio` provider는 `/v1/models` 조회 뒤 `/api/v0/models`를 추가 조회하고, `max_context_length`를 같은 모델 ID 또는 경로 표시명 기준으로 병합합니다.
  - **Unit 10-3 기본 생성값 정정:** 새 세션 기본값을 `temperature=1`, `top_p=0.95`, `top_k=40`, `repetition_penalty=1.1`로 고정합니다.
  - **Unit 10-4 입력 한도 기준 압축:** 컨텍스트 사용량 바와 압축 트리거를 `contextLength`가 아니라 `maxInputTokens` 기준으로 계산합니다.
  - **Unit 10-5 수동 보정 UI:** 설정 모달 일반 설정 탭에 전체 컨텍스트, 입력 한도, 출력 한도 입력과 저장/제거 버튼을 제공하고 provider/model별로 보존합니다.
  - **Unit 10-6 문서 동기화:** v1.3.1 변경 기준을 `spec.md`, `designs.md`, `README.md`, `CHANGELOG.md`, `DESIGN_DECISIONS.md`, `implementation_summary.md`, `BUILD_GUIDE.md`, `LESSONS_LEARNED.md`, `audit_roadmap.md`에 반영합니다.
* **검증 포인트:**
  - `proxy_test.go`에서 메타데이터 없는 OpenAI 호환 모델이 `8192`로 보정되지 않아야 합니다.
  - `proxy_test.go`에서 LM Studio `max_context_length`가 `ContextLength`와 `MaxInputTokens`로 병합되어야 합니다.
  - UI 초기값은 Temp `1`, Top P `0.95`, Top K `40`, RP `1.1`이어야 합니다.
  - 모델 한도를 알 수 없을 때 입력창 위 컨텍스트 바와 우측 패널은 `알 수 없음` 및 `수동 보정 필요`를 표시해야 합니다.

### 2.11 Phase 11: API 채팅 플랫폼 확장 (v1.4.0 구현 완료)
* **목표:** LiteFlashChat을 순수 API 기반 채팅 플랫폼으로 강화하기 위해 스트리밍 응답, 요청 취소, 대화 데이터 관리, 검색, Inspector, 프리셋을 하나의 일관된 로컬 플랫폼 흐름으로 완성했습니다.
* **구현 단위:**
  - **Unit 11-1 문서 계약 동결:** `spec.md`, `designs.md`, `implementation_summary.md`, `DESIGN_DECISIONS.md`, `BUILD_GUIDE.md`, `LESSONS_LEARNED.md`, `README.md`, `CHANGELOG.md`, `audit_roadmap.md`에 v1.4.0 계약을 먼저 반영했습니다.
  - **Unit 11-2 스트리밍 백엔드:** `/api/chat/stream`을 추가하고 OpenAI 호환 SSE의 `delta.content`, `[DONE]`, 원격 오류, malformed chunk를 처리합니다.
  - **Unit 11-3 스트리밍 프론트:** assistant placeholder 메시지, chunk 누적, `[전송] -> [중지]` 전환, `complete/cancelled/error` 메시지 상태를 구현했습니다.
  - **Unit 11-4 데이터 관리:** `ChatExportBundle` JSON 내보내기/가져오기와 Markdown 내보내기를 구현하고, 가져오기는 기존 데이터를 삭제하지 않는 병합으로 고정했습니다.
  - **Unit 11-5 대화 검색:** 좌측 사이드바에서 제목, 본문, provider, model, Super Prompt 이름 기준으로 세션 목록을 필터링합니다.
  - **Unit 11-6 Inspector:** 마지막 `/api/chat/stream` 요청/응답의 sanitized snapshot을 우측 패널에 표시하고 복사 버튼을 제공합니다.
  - **Unit 11-7 프리셋:** provider, model, Super Prompt, generation settings, compression ratio, model override를 `ChatPreset`으로 저장/적용/이름 변경/삭제합니다.
  - **Unit 11-8 문서/패키징 동기화:** v1.4.0 완료 상태로 모든 표준 문서와 `package.json`, `package-lock.json`을 갱신했습니다.
* **동결 정책:**
  - 기본 채팅 전송은 `/api/chat/stream`을 사용합니다.
  - 스트리밍 실패 시 `/api/chat`으로 자동 폴백하지 않습니다.
  - Inspector와 내보내기는 API Key, Authorization, `.secret.key`, `keys.json` 암호문을 절대 포함하지 않습니다.
  - JSON 가져오기는 병합만 수행하고 기존 세션을 자동 삭제하지 않습니다.
  - 프리셋은 API Key와 Base URL을 포함하지 않습니다.
* **검증 포인트:**
  - 스트리밍 응답이 하나의 assistant 메시지에 순서대로 누적되어야 합니다.
  - `[중지]` 클릭 후 부분 응답이 보존되고 메시지 상태가 `cancelled`여야 합니다.
  - JSON export/import 후 세션 ID 충돌이 새 ID로 병합되어야 합니다.
  - Markdown export는 사람이 읽을 수 있는 대화 로그를 생성해야 합니다.
  - 검색은 제목/본문/provider/model/Super Prompt 기준으로 결과를 갱신해야 합니다.
  - Inspector 복사 결과에 secret 계열 값이 없어야 합니다.
  - 프리셋 적용 후 현재 세션의 provider/model/prompt/settings/compression/override가 반영되어야 합니다.

### 2.12 Phase 12: 구조 정리 및 God Component 해소 (완료)
* **목표:** v1.4.0 기능을 유지한 채 `src/App.tsx`, `src/index.css`, `main.go`, `proxy.go`의 과도한 책임 집중을 단계적으로 해소합니다.
* **현재 기준선:**
  - `src/App.tsx`: 기준 2439줄 -> 862줄
  - `src/index.css`: 기준 1076줄 -> 82줄
  - `main.go`: 기준 592줄 -> 62줄
  - `proxy.go`: 기준 723줄 -> 3줄 안내 파일
* **구현 단위:**
  - **Unit 12-1 기준선 고정:** 현재 기능 목록, 줄 수, 품질 게이트 결과를 기록합니다.
  - **Unit 12-2 순수 유틸 분리:** 완료. 토큰 추정, 파일 다운로드, export/import, localStorage, 모델 override, Inspector sanitize, provider 표시 함수를 `src/utils/*`로 이동했습니다.
  - **Unit 12-3 API 클라이언트 분리:** 완료. provider/model/prompt fetch 경계는 `src/api/client.ts`, stream/summary 실행 경계는 `src/services/chatFlow.ts`로 이동했습니다.
  - **Unit 12-4 상태 훅/서비스 분리:** 완료. 세션/압축률/모델 보정/프리셋/Inspector 상태는 `src/hooks/*`, 채팅 턴 실행과 데이터 이식은 `src/services/*`로 분리했습니다.
  - **Unit 12-5 UI 컴포넌트 분리:** 완료. Sidebar, ChatHeader, MessageList, InputPanel, SettingsModal, ModelSearchModal, ParamsPanel을 `src/components/*`로 분리했습니다.
  - **Unit 12-6 스타일 분리:** 완료. `src/index.css`는 전역 토큰/import 중심으로 유지하고 기능별 CSS를 `src/styles/*`로 분리했습니다.
  - **Unit 12-7 백엔드 파일 분리:** 완료. `models.go`, `chat_proxy.go`, `stream_proxy.go`, `summary.go`, `system_prompt.go` 및 핸들러/서버 보조 파일로 알고리즘 책임을 나눴습니다.
  - **Unit 12-8 문서/검증 마감:** 완료. 파일 책임표, ADR, changelog, 로드맵 완료 상태를 갱신하고 게이트를 통과해야 합니다.
* **동결 정책:**
  - Phase 12는 동작 변경이 아닌 구조 변경입니다.
  - API endpoint, JSON schema, localStorage key, provider ID, prompt 저장 형식, export schemaVersion은 변경하지 않습니다.
  - Redux/Recoil 등 새 전역 상태 라이브러리는 도입하지 않습니다.
  - 각 단위는 가능한 한 작은 패치로 완료하고, 실패 시 이전 단위의 통과 상태로 돌아갈 수 있어야 합니다.
* **검증 포인트:**
  - 각 단위 완료 후 `npm run lint`, `npm run build`, `go test ./...`, `npm run build:output` 중 영향 범위에 맞는 명령이 통과해야 합니다.
  - 전체 완료 시 네 명령이 모두 통과해야 합니다.
  - v1.4.0 수동 감사 순서가 기존과 동일하게 통과해야 합니다.
  - 완료 목표는 `src/App.tsx` 900줄 이하, `src/index.css` 350줄 이하입니다. 단, 기능 회귀 방지가 줄 수 목표보다 우선합니다.

---

## 3. 체크포인트 감사 정책 (Checkpoint Policy)
각 Phase가 완료될 때마다 다음 터미널 명령어와 수동 감사 과정을 거치지 않으면 다음 페이즈의 어떠한 코드 변경도 시도되지 않음을 보장합니다.

* **Phase 1~2 체크포인트 검증 명령:**
  ```bash
  go test -v ./...
  ```
* **Phase 5 체크포인트 검증 명령:**
  ```bash
  curl -I http://localhost:8080/api/prompts
  ```
* **Phase 6 체크포인트 검증 명령:**
  ```bash
  go test -v ./...
  npm run lint
  npm run build
  go build -buildvcs=false -o /tmp/liteflashchat-audit .
  ```
* **Phase 7 체크포인트 검증 명령:**
  ```bash
  npm run build:output
  ```
* **Phase 8 체크포인트 검증 명령:**
  ```bash
  npm run lint
  npm run build
  go test ./...
  npm run build:output
  ```
* **Phase 9 체크포인트 검증 명령:**
  ```bash
  npm run lint
  npm run build
  go test ./...
  npm run build:output
  ```
* **Phase 11 체크포인트 검증 명령:**
  ```bash
  npm run lint
  npm run build
  go test ./...
  npm run build:output
  ```
* **Phase 12 체크포인트 검증 명령:**
  ```bash
  npm run lint
  npm run build
  go test ./...
  npm run build:output
  ```
* **Phase 9 수동 감사 순서:**
  1. OpenRouter 또는 fixture 기반 모델 목록에서 컨텍스트/출력 한도/지원 파라미터 표시를 확인합니다.
  2. 모델 파라미터 미지원 항목이 disabled 상태이며 payload에 포함되지 않는지 네트워크 덤프로 확인합니다.
  3. 새 대화 첫 메시지 제목 자동 저장과 제목 수동 변경 보존을 새로고침 후 확인합니다.
  4. 압축 비율 `0.7`에서 긴 대화가 요약 요청을 먼저 수행하는지 확인합니다.
* **Phase 11 수동 감사 순서:**
  1. 실제 provider 또는 stream fixture로 `/api/chat/stream` 응답이 점진 표시되는지 확인합니다.
  2. 스트리밍 중 `[중지]`를 눌러 부분 응답 보존과 `cancelled` 상태를 확인합니다.
  3. JSON 내보내기 파일에 API Key/Authorization/암호문이 없는지 확인하고, 가져오기 병합 후 기존 대화가 유지되는지 확인합니다.
  4. Markdown 내보내기 결과가 제목, provider/model, user/assistant 메시지를 읽기 좋게 포함하는지 확인합니다.
  5. 사이드바 검색어로 제목/본문/provider/model/Super Prompt 매칭 대화만 남는지 확인합니다.
  6. Inspector에서 sanitized request/response 복사 결과에 secret이 없는지 확인합니다.
  7. 프리셋 저장 후 새 대화에 적용하여 provider/model/prompt/settings/compression/override가 반영되는지 확인합니다.
* **Phase 12 수동 감사 순서:**
  1. 리팩터링 전후 동일한 provider 저장, 모델 조회, 모델 선택, Super Prompt 적용, 스트리밍 채팅 흐름을 확인합니다.
  2. JSON/Markdown 내보내기와 JSON 가져오기 병합 결과가 동일한 schemaVersion과 저장 키를 유지하는지 확인합니다.
  3. Inspector 복사 결과의 secret 마스킹이 유지되는지 확인합니다.
  4. 프리셋 저장/적용/이름 변경/삭제가 리팩터링 전과 같은 상태 전이를 갖는지 확인합니다.
