# 기술 의사결정 레코드 (DESIGN_DECISIONS.md)

본 문서는 LiteFlashChat 아키텍처 수립 및 개발 과정에서 논의된 핵심 기술 의사결정 내역을 보존하는 기록서(Architecture Decision Records, ADR)입니다. (v1.4.0 구현 완료 및 Phase 12 구조 정리 로드맵 확정 - API 채팅 플랫폼 확장 ADR 반영)

---

## 1. [ADR-001] 로컬 마스터키 없는 자동 암호화 저장 방식 채택

### 1.1 배경 (Context)
* 사용자의 AI API 키(특히 OpenRouter 등)는 유출 시 큰 금전적 피해가 발생하므로 디스크에 평문으로 보관할 수 없습니다.

### 1.2 결정 (Decision)
* **Go 백엔드가 구동 시 로컬 기기에 `.secret.key` 암호화 마스터키 파일을 32바이트 보안 난수로 자동 생성하고 보관**하는 방식을 채택합니다.

---

## 2. [ADR-002] 백엔드 단독 통신 중계(API 프록싱) 구조 도입

### 2.1 배경 (Context)
* 브라우저 프론트엔드 단독으로 외부 AI 서버 직접 연동 시 발생하는 CORS 문제 및 API 키 노출 위협을 차단해야 합니다.

### 2.2 결정 (Decision)
* **모든 API 요청을 Go 백엔드 서버(`/api/chat`, `/api/models`)로 집결**시켜, Go 서버 측에서 원격 백엔드로 중계 및 프록싱하는 구조를 고정 동결합니다.

---

## 3. [ADR-003] ⚙️ 우측 상단 설정 레이어 분리 통합 결정

### 3.1 배경 (Context)
* 사이드바 내부에 프로바이더 설정 버튼들이 산재해 있으면 UI 레이아웃이 복잡해지고, 향후 추가적인 프로바이더(LM Studio, Local LLM 등) 유입 시 비주얼 엑설런스가 심각하게 저하됩니다.

### 3.2 결정 (Decision)
* 설정 영역을 완전히 격리하여 화면 우측 상단 톱니바퀴(`⚙️`) 버튼 하나로 집결시키고, 클릭 시 탭 스위치(프로바이더 설정 탭, Super Prompt 관리 탭)를 연동하는 모던 반투명 모달 설정 레이어를 단독 가동하기로 결정합니다.

---

## 4. [ADR-004] Super Prompt 평문 prompts.json 파일 보존 결정

### 4.1 배경 (Context)
* 사용자가 등록하는 Super Prompt(시스템 프롬프트)는 API 키와 달리 외부 유출 시 어떠한 금전적이나 물리적인 피해를 입히지 않는 순수 대화용 지침 텍스트 자산입니다.
* 이를 굳이 대칭키 암호화하여 저장하면, 파일이 부분 오염되었을 때 복원이 불가능해지고 수동으로 백업하거나 텍스트 편집기에서 수정하기가 대단히 곤란해집니다.

### 4.2 결정 (Decision)
* Super Prompt 데이터베이스는 평문 JSON 형태의 `prompts.json` 파일에 저장하여, 사용자가 언제든 수동으로 프롬프트 백업이나 외부 수정이 용이하게끔 격리 관리하기로 결정합니다.

---

## 5. [ADR-005] [v1.2.0] 검색 필터식 모델 탐색기 모달 도입 결정

### 5.1 배경 (Context)
* 수십 개에서 수백 개에 달하는 OpenRouter 모델 목록을 단순한 드롭다운 select 메뉴로 서빙하면 사용자가 스크롤하며 원하는 모델을 찾는 탐색 피로도가 극에 달합니다. 
* 또한 모델별 Vision 멀티모달 지원 여부를 텍스트 태그로만 표현하는 것은 시각적 프리미엄 미학(Visual Excellence)에 미치지 못합니다.

### 5.2 결정 (Decision)
* **글래스모피즘(Glassmorphism) 기반의 독자적인 '모델 탐색기 모달(Model Explorer Modal)'을 설계**합니다.
* 사용자가 1글자 입력할 때마다 실시간으로 100ms 이내에 모델 카드 목록이 정교하게 필터링되는 반응형 검색창을 내장하고, Vision/TextOnly 여부를 시각적 파스텔 톤 배지로 구분 마킹하며, 카드 더블클릭 시 즉시 대화방에 장착하는 빠른 인터페이스를 구축하기로 동결 결정합니다.

---

## 6. [v1.2.1] [ADR-006] 수정 모드 내 안전 키 유지(__KEEP_EXISTING__ 플래그) 설계 결정

### 6.1 배경 (Context)
* 이미 API Key가 연동되어 설정 모달에서 활성 상태로 보이는 프로바이더의 설정을 수정하고자 할 때, API Key 원본은 백엔드 내부에만 암호화 보관되고 프론트엔드는 원본 키를 수신하지 않는 상태(Zero-Exposure)입니다.
* 사용자가 Base URL만 변경하려 해도 빈 Key 상태이므로 저장 버튼이 비활성화되거나, 매번 긴 API Key 원본을 복사해서 다시 입력해야 하는 악질적인 UX가 발생합니다.
* 이를 해결하기 위해 백엔드에서 키를 복호화해 브라우저 인풋 폼에 사전에 담아 보여주자는 대안이 제시되었으나, 브라우저 메모리에 원본 API Key가 상주하게 되어 XSS 등 유출 위험(Security Leak)이 극적으로 상승하므로 **기각**하였습니다.

### 6.2 결정 (Decision)
* **`__KEEP_EXISTING__` 가상 안전 암호화 변경 지시 지침 플래그 규격을 설계**합니다.
* 프론트엔드 설정 오버레이에서 수정 모드(`isProviderEditMode === true`) 감지 상태 기계를 적용합니다.
* 사용자가 API Key 입력창을 빈 칸으로 두고 Base URL만 갱신하거나 API Key 일부만 입력하면, 프론트엔드는 `api_key: "__KEEP_EXISTING__"` 신호를 백엔드로 포스팅합니다.
* 백엔드는 이 플래그를 수신 시, **기존 디스크에 저장되어 있던 안전한 암호화 Key와 IV 블록을 그대로 보존(Keep)**하면서 Base URL 파라미터만 원자적 교체 연동하도록 동결 구현하여 최상의 보안 경계와 고도화된 UX 편의성을 동시에 실현합니다.

---

## 7. [v1.2.2] [ADR-007] 키 보존 테스트 격리 및 기존 키 검증 결정

### 7.1 배경 (Context)
* 이전 테스트는 `.secret.key` 자동 생성 지속성을 검증하기 위해 실제 프로젝트 루트의 `.secret.key`를 삭제할 수 있었습니다.
* 실제 `keys.json`은 `.secret.key`와 쌍을 이루므로, 테스트가 키 파일을 재생성하면 기존 암호문이 모두 복호화 불가능한 상태가 됩니다.

### 7.2 결정 (Decision)
* 암호화 테스트는 테스트 전용 임시 키 파일 경로를 주입하여 실제 사용자 `.secret.key`를 절대 삭제하지 않습니다.
* `__KEEP_EXISTING__` 저장은 기존 암호문을 먼저 복호화해 무결성을 확인한 뒤에만 허용합니다.
* 복호화 실패 시 기존 키 유지를 거부하고 API Key 재등록이 필요한 `invalid_key` 상태로 노출합니다.

---

## 8. [v1.2.2] [ADR-008] Base URL API 루트 정규화 결정

### 8.1 배경 (Context)
* 사용자가 Base URL 입력란에 `/chat/completions` 또는 `/models`까지 포함한 전체 엔드포인트를 저장하면 백엔드가 다시 하위 경로를 붙여 모델 조회와 채팅 프록시가 모두 실패합니다.

### 8.2 결정 (Decision)
* 저장되는 Base URL은 API 루트만 허용합니다.
* 백엔드는 저장 전에 `/models`, `/chat/completions` 접미사를 제거해 API 루트로 정규화합니다.
* 지원하지 않는 provider, 스킴 없는 URL, query string, fragment는 저장을 거부합니다.

---

## 9. [v1.2.2] [ADR-009] 로컬 프로바이더 무키 저장 및 상태 진단 결정

### 9.1 배경 (Context)
* LM Studio 및 OpenAI 호환 로컬 서버는 API Key가 필요 없거나 임의 문자열을 허용하는 경우가 많습니다.
* 모든 프로바이더에 동일하게 API Key 필수 조건을 적용하면 로컬 서버 사용자가 Base URL만 저장하고 모델을 불러오는 자연스러운 흐름이 막힙니다.

### 9.2 결정 (Decision)
* `lm_studio`, `local_llm`은 API Key 입력 없이도 저장 가능하게 하며, 백엔드는 내부 더미 토큰을 암호화 저장해 요청 헤더 구조를 일관되게 유지합니다.
* `/api/providers`는 `configStatus`와 `statusMessage`를 반환하여 UI가 `missing`, `ready`, `invalid_key`를 구분해 표시합니다.

---

## 10. [v1.2.4] [ADR-010] 설정 저장 즉시 반영 및 Super Prompt 책임 분리 결정

### 10.1 배경 (Context)
* 웹 인터페이스에서 프로바이더 저장 후 모델 목록이 바로 갱신되지 않으면 사용자는 설정이 성공했는지 판단하기 위해 브라우저 새로고침에 의존하게 됩니다.
* 좌측 하단에 프로바이더명, Edit 버튼, 활성 LED가 별도로 존재하면 같은 설정 상태가 설정 모달과 사이드바에 중복 표시되어 UI 책임이 분산됩니다.
* Super Prompt 목록에서 바로 대화방 장착까지 수행하면, 프롬프트를 편집하려는 행동과 현재 대화방에 적용하려는 행동이 같은 화면 안에서 섞입니다.

### 10.2 결정 (Decision)
* 프로바이더 저장 성공 후 프론트엔드는 `/api/providers`를 즉시 재조회하고 저장 대상 provider를 `activeProvider`, 현재 세션 `provider`, 모델 목록 재조회 흐름에 연결합니다.
* 사이드바의 provider/Edit/LED 목록은 제거하고, 프로바이더 상태와 오류 메시지는 설정 모달의 provider 상태 카드에서만 확인합니다.
* Super Prompt 설정 모달은 생성, 편집 폼 적재, 저장, 삭제만 담당합니다.
* 대화방 장착과 해제는 사이드바 Super Prompt 패널의 선택 박스, `[적용]`, `[해제]` 버튼에서만 수행합니다.

### 10.3 대안과 기각 사유
* **대안 A: 저장 후 사용자가 새로고침하도록 안내** - 웹 앱 UX 기준에 맞지 않고, 모델 조회 실패와 저장 성공을 구분하기 어렵기 때문에 기각했습니다.
* **대안 B: 사이드바 provider 목록 유지** - 동일 상태가 두 화면에 중복되어 문서와 구현의 단일 책임 원칙을 깨므로 기각했습니다.
* **대안 C: 설정 모달에서 Super Prompt 장착 유지** - CRUD와 세션 적용 책임이 결합되어 현재 대화방 상태를 오인할 가능성이 커서 기각했습니다.

### 10.4 결과 (Consequences)
* 설정 저장, 모델 조회, 모델 선택 흐름이 새로고침 없이 순환합니다.
* 프로바이더 상태 확인 위치가 설정 모달로 단일화되어 문서와 UI가 같은 책임 경계를 갖습니다.
* Super Prompt 편집과 적용이 분리되어, 후속 기능 추가 시 CRUD 저장소와 세션 적용 상태를 독립적으로 점검할 수 있습니다.

---

## 11. [v1.3.0] [ADR-011] 확정 메타데이터 기반 모델 파라미터 및 LLM 요약 압축 결정

### 11.1 배경 (Context)
* 사용자 요구사항은 모델 선택 시 최대 컨텍스트와 입/출력 토큰 한도를 적용하고, 우측 패널에서 `temperature`, `top_p`, `top_k`, `repetition_penalty`를 설정하는 것입니다.
* OpenRouter 모델 API는 모델별 컨텍스트와 지원 파라미터를 제공하지만, 모든 OpenAI 호환 로컬 서버가 동일한 메타데이터를 제공한다고 보장할 수 없습니다.
* 지원 여부가 불명확한 파라미터를 무조건 전송하면 일부 모델/서버가 요청을 거부할 수 있고, 사용자는 설정값이 적용됐는지 판단하기 어렵습니다.
* 컨텍스트 초과 문제는 오래된 메시지를 단순 삭제하면 대화 의미가 사라지고, 조용한 누락은 사용자가 결과를 신뢰하기 어렵게 만듭니다.

### 11.2 결정 (Decision)
* 모델 파라미터 지원 여부는 **확실할 때만 활성화**합니다. `supportedParameters` 또는 동등한 원격 메타데이터가 없는 항목은 UI에서 disabled 처리하고 `/api/chat` payload에서 제외합니다.
* OpenRouter는 `context_length`, `top_provider.max_completion_tokens`, `supported_parameters`를 파싱하여 `ModelInfo`에 반영합니다.
* OpenAI 호환 계열은 응답에 명시된 필드만 사용하며, v1.3.1 이후 정보가 없으면 컨텍스트를 `알 수 없음`으로 표시하고 임의 기본값을 적용하지 않습니다.
* 컨텍스트 압축은 기본 비율 `0.7`을 사용하고, 오래된 대화 묶음을 동일 provider/model로 **LLM 요약**하여 `ChatSession.contextSummary`에 저장합니다.
* 요약 실패 시 실제 채팅 발송을 중단하고 사용자에게 오류를 표시합니다. 오래된 메시지를 조용히 삭제하거나 임의 절단하지 않습니다.

### 11.3 대안과 기각 사유
* **대안 A: 모든 모델에서 모든 샘플링 파라미터 활성화** - 원격 API별 허용 파라미터 차이로 실패 가능성이 높아 기각했습니다.
* **대안 B: provider 단위 추정으로 활성화** - 동일 provider 안에서도 모델별 지원 차이가 있을 수 있어 기각했습니다.
* **대안 C: 로컬 절단 기반 컨텍스트 압축** - 추가 API 호출은 없지만 대화 의미 손실을 사용자가 확인하기 어려우므로 v1.3.0 기본 전략에서 기각했습니다.

### 11.4 결과 (Consequences)
* UI는 "적용 가능한 설정만 조작 가능"한 상태가 되어 원격 API 오류를 줄입니다.
* 모델 메타데이터가 부족한 로컬 서버는 보수적으로 동작하므로 일부 고급 파라미터가 비활성화될 수 있습니다.
* LLM 요약은 추가 호출 비용과 지연이 있지만, 대화 의미를 보존한 상태로 컨텍스트 한도 내 전송을 시도할 수 있습니다.

---

## 12. [v1.3.1] [ADR-012] 임의 컨텍스트 폴백 제거 및 수동 모델 한도 보정 결정

### 12.1 배경 (Context)
* 사용자는 모델의 최대 컨텍스트와 입력/출력 토큰값이 실제 모델 메타데이터에서 와야 하며, `8192` 같은 값이 출력 컨텍스트 또는 임의 추정값처럼 보이는 문제를 지적했습니다.
* LM Studio는 OpenAI 호환 `/v1/models` 응답만으로 컨텍스트가 충분히 드러나지 않을 수 있지만, 별도 `/api/v0/models` 응답에는 `max_context_length`가 포함될 수 있습니다.
* 모든 Local LLM 서버가 동일한 메타데이터 규격을 제공하지 않으므로, 확정 불가능한 값을 확정값처럼 표시하면 사용자가 모델 한도와 압축 동작을 잘못 이해하게 됩니다.

### 12.2 결정 (Decision)
* OpenAI 호환 모델 응답에 컨텍스트 필드가 없으면 더 이상 `8192` 같은 폴백값을 만들지 않습니다.
* LM Studio provider는 `/v1/models` 조회 뒤 `/api/v0/models`를 추가 조회하여 `max_context_length`를 병합합니다.
* 모델 정보에는 `metadataSource`와 `isContextEstimated`를 포함하여 값의 출처와 확정 여부를 UI에 노출합니다.
* 설정 모달 일반 설정 탭에 provider/model별 전체 컨텍스트, 입력 한도, 출력 한도 수동 보정을 제공하고, 수동값은 `metadataSource=manual`로 표시합니다.
* 새 세션의 기본 생성 파라미터는 `temperature=1`, `top_p=0.95`, `top_k=40`, `repetition_penalty=1.1`로 고정합니다.
* 압축 기준은 전체 컨텍스트가 아니라 입력 한도인 `maxInputTokens * compressionRatio`로 계산합니다.

### 12.3 대안과 기각 사유
* **대안 A: 기존 8192 폴백 유지** - 실제 모델 한도와 다를 가능성이 높고 사용자가 지적한 혼동을 해결하지 못해 기각했습니다.
* **대안 B: 모델명 기반 하드코딩 테이블 도입** - 모델 배포판과 양자화 파일명마다 한도가 달라질 수 있어 유지보수 비용과 오탐 위험이 커서 기각했습니다.
* **대안 C: 모든 Local LLM에 LM Studio v0 API 강제 적용** - OpenAI 호환 서버마다 v0 API 존재 여부가 다르므로 `lm_studio` provider에만 제한합니다.

### 12.4 결과 (Consequences)
* UI가 확정된 모델 한도와 알 수 없는 한도를 명확히 구분합니다.
* LM Studio 사용자는 실제 로드된 모델의 `max_context_length`를 더 정확히 볼 수 있습니다.
* 메타데이터가 부족한 서버도 사용자가 수동 보정값을 입력하면 압축과 컨텍스트 표시를 실사용 기준에 맞출 수 있습니다.

---

## 13. [v1.4.0] [ADR-013] API 채팅 플랫폼 확장 기능 묶음 결정

### 13.1 배경 (Context)
* 사용자는 LiteFlashChat을 순수하게 API를 받아 채팅하는 플랫폼으로 사용할 때 추가하면 좋은 기능을 문의했고, 스트리밍 외에도 데이터 관리, 검색, 요청/응답 검사, 프리셋을 함께 추진하기로 확정했습니다.
* 현재 구조는 로컬 단일 사용자, Go 프록시, React 상태, `localStorage` 저장을 기준으로 안정화되어 있으므로, v1.4.0은 외부 DB나 계정 시스템 없이 플랫폼 사용성을 높이는 방향이 적합합니다.
* API 기반 채팅 플랫폼은 응답 체감 속도, 백업 가능성, 누적 대화 탐색, 실제 요청 디버깅, 반복 설정 재사용이 핵심 사용성입니다.

### 13.2 결정 (Decision)
* v1.4.0 기능 묶음은 스트리밍 응답/요청 취소, JSON/Markdown 내보내기, JSON 가져오기, 대화 검색, Inspector, 프리셋으로 구현했습니다.
* 기본 채팅 전송은 `/api/chat/stream`으로 전환하고, 실패 시 `/api/chat` 자동 폴백은 하지 않습니다.
* 요청 취소는 브라우저 `AbortController`를 사용하며, 부분 응답은 삭제하지 않고 `ChatMessage.status="cancelled"`로 보존합니다.
* JSON 내보내기는 복원용, Markdown 내보내기는 읽기용으로 역할을 분리하고, Markdown 가져오기는 지원하지 않습니다.
* JSON 가져오기는 기존 데이터를 삭제하지 않는 병합 방식으로 고정하며, 세션 ID 충돌 시 새 ID를 부여합니다.
* Inspector는 마지막 요청/응답 snapshot만 런타임 상태에 보관하고, API Key, Authorization, `.secret.key`, `keys.json` 암호문은 표시하지 않습니다.
* 프리셋은 provider, model, Super Prompt, generation settings, compression ratio, model override만 포함하며 API Key와 Base URL은 포함하지 않습니다.

### 13.3 대안과 기각 사유
* **대안 A: 스트리밍만 단독 구현** - 체감 개선은 크지만 백업, 검색, 디버깅, 반복 설정 재사용 문제가 남아 플랫폼 완성도가 낮아 기각했습니다.
* **대안 B: 원격 DB/계정 기반 플랫폼화** - 현재 프로젝트의 로컬 단일 사용자 보안 경계를 크게 바꾸고 구현 범위가 과도해 v1.4.0 범위에서 기각했습니다.
* **대안 C: 스트리밍 실패 시 자동 non-stream 폴백** - 사용자가 실제 요청 방식과 오류 원인을 오해할 수 있어 기각했습니다.
* **대안 D: Inspector 영구 저장** - 디버깅 편의는 있지만 민감한 요청 내용이 장기 보존될 수 있어 런타임 상태 보관으로 제한했습니다.

### 13.4 결과 (Consequences)
* LiteFlashChat은 단순 채팅 클라이언트에서 로컬 API 채팅 운영 도구에 가까워집니다.
* 스트리밍, Inspector, 프리셋이 모두 같은 payload 생성 경로를 공유해야 하므로 타입과 테스트 정합성이 더 중요해집니다.
* 외부 DB를 도입하지 않으므로 구현 복잡도는 억제되지만, 매우 큰 대화 이력에서는 클라이언트 검색 성능 한계가 남습니다.

---

## 14. [Phase 12] [ADR-014] 무회귀 구조 정리 로드맵 결정

### 14.1 배경 (Context)
* v1.4.0까지 스트리밍, 데이터 관리, 검색, Inspector, 프리셋이 추가되면서 `src/App.tsx`가 2439줄까지 커졌습니다.
* `src/index.css`도 1076줄로 커져 UI 영역별 스타일 책임을 추적하기 어려워졌습니다.
* 백엔드도 `main.go`와 `proxy.go`에 라우팅, 모델 조회, 채팅 프록시, stream, summary, system prompt 주입이 집중되어 있습니다.
* 프로젝트의 목적이 API 모델 테스트 및 채팅 플랫폼이라면 이후 다중 모델 비교, 테스트 로그, 요청 덤프 같은 기능이 추가될 가능성이 높으므로, 지금 구조를 그대로 확장하면 회귀 위험이 커집니다.

### 14.2 결정 (Decision)
* Phase 12는 기능 추가가 아니라 **동작 무변경 구조 정리**로 동결합니다.
* 첫 단계는 순수 유틸 분리이며, API 클라이언트, 상태 훅, UI 컴포넌트, 스타일, 백엔드 파일 분리 순서로 진행합니다.
* 새 전역 상태 라이브러리는 도입하지 않고 React hook과 props 전달로 책임을 나눕니다.
* 공개 API endpoint, JSON schema, localStorage key, provider ID, export schemaVersion, 저장 파일 형식은 유지합니다.
* 각 단위는 독립 검증을 통과해야 다음 단위로 넘어갑니다.

### 14.3 대안과 기각 사유
* **대안 A: 전면 대분리 일괄 수행** - 변경 범위가 커져 v1.4.0 스트리밍/백업/Inspector 기능 회귀를 찾기 어려우므로 기각했습니다.
* **대안 B: Redux/Zustand 등 상태 라이브러리 도입** - 현재 프로젝트 원칙인 React 기본 상태 중심 경계를 바꾸고 문서/테스트 범위를 크게 늘리므로 Phase 12에서는 기각했습니다.
* **대안 C: 현 구조 유지** - 단기 속도는 빠르지만 모델 테스트 플랫폼 기능을 추가할수록 `App.tsx` 중심 회귀 위험이 커지므로 기각했습니다.

### 14.4 결과 (Consequences)
* 리팩터링 완료 후 `src/App.tsx`는 화면 조립자 역할로 축소되고, 기능별 테스트/수정 범위가 명확해집니다.
* CSS와 백엔드 프록시 책임이 나뉘어 후속 모델 테스트 기능을 추가할 때 영향 범위를 줄일 수 있습니다.
* 구조 정리 동안에는 기능 개발을 보류하고, 품질 게이트와 v1.4.0 수동 감사 순서를 우선합니다.
* 2026-05-28 정리 완료 시점에는 `src/App.tsx`를 862줄, `src/index.css`를 82줄, `main.go`를 62줄로 축소하고, UI 컴포넌트, 상태 hook, 채팅/데이터 service, CSS, 백엔드 프록시 책임을 파일 단위로 분리했습니다.
